(executables
 ((names (simple timeouts test_zombies errors))
  (libraries (async
              rpc_parallel_core_deprecated
              core
              jane
              sexplib))
  (preprocess (pps (ppx_jane)))))

(jbuild_version jane_street)
