(library (
  (name        rpc_parallel)
  (public_name rpc_parallel)
  (libraries (async
              core
              core.daemon
              sexplib
              core.uuid))
  (preprocess (pps (ppx_jane ppxlib.runner)))))



(jbuild_version 1)
