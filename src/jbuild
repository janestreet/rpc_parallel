(library (
  (name        rpc_parallel)
  (public_name rpc_parallel)
  (flags (:standard -safe-string))
  (libraries (core async sexplib))
  (preprocess (pps (ppx_jane ppx_driver.runner)))))


(jbuild_version 1)
